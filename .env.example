DATABASE_URL="postgresql://..."
DIRECT_URL="postgresql://..."

# Required in production. Generate a strong random value (for example, 32+ bytes).
AUTH_SECRET="replace-with-a-strong-random-secret"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""

# Required only when /api/sign-pdf is used.
PDF_CERT_PASSPHRASE=""
# Base64-encoded contents of certificate.p12. Do not commit the certificate or its value.
PDF_CERT_BASE64=""

# Optional for prisma db seed.
ADMIN_INITIAL_PASSWORD=""
